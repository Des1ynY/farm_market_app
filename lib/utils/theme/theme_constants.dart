import 'package:flutter/material.dart';

// Constraints contants
const kPaddingDefault = EdgeInsets.symmetric(
  vertical: 40,
  horizontal: 20,
);
const kPageSmallPadding = EdgeInsets.all(20);
const kAppBarHeight = 42.0;
